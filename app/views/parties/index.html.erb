<% if @category %>
    <h3><%= @category.name %></h3>
<% end %>
<h1>All of the parties!! </h1>
<ul>
    <% @parties.each do |party|%>
        <li><%= link_to party.name, party_path(party)%></li>
    <% end %>
</ul>


<%= link_to "add new party", new_party_path%>