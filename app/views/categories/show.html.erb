<h2><%= @category.name %></h2>

<h3>All parties under  <%= @category.name %> category:</h3>
<ul>
    <%@parties.each do |party| %>
        <li><%= link_to party.name, party_path(party) %></li>
    <% end %>

</ul>